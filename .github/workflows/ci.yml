name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".cspell/**"
      - ".github/workflows/ci.yml"
      - ".markdownlint*"
      - ".mvn/**"
      - ".spectral.yaml"
      - "*.md"
      - "config/**"
      - "docs/**"
      - "mvnw"
      - "mvnw.cmd"
      - "pom.xml"
      - "searchable-*/pom.xml"
      - "searchable-*/src/**"
  pull_request:
    branches: [main]
    paths:
      - ".cspell/**"
      - ".github/workflows/ci.yml"
      - ".markdownlint*"
      - ".mvn/**"
      - ".spectral.yaml"
      - "*.md"
      - "config/**"
      - "docs/**"
      - "mvnw"
      - "mvnw.cmd"
      - "pom.xml"
      - "searchable-*/pom.xml"
      - "searchable-*/src/**"

permissions:
  contents: read

env:
  MAVEN_OPTS: "-Xmx2g -Dfile.encoding=UTF-8"
  MAVEN_CLI_OPTS: "--batch-mode --no-transfer-progress --errors --fail-at-end -Dstyle.color=always"

jobs:
  build-and-test:
    name: Build + Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build and run unit tests
        run: ./mvnw $MAVEN_CLI_OPTS -DskipITs verify

      - name: Upload Surefire reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: surefire-reports
          path: "**/target/surefire-reports/"

  integration-test:
    name: Integration Tests (H2 + Testcontainers)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run full test suite + aggregate coverage
        run: ./mvnw $MAVEN_CLI_OPTS -Pcoverage-aggregate verify

      - name: Upload Surefire reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: integration-surefire-reports
          path: "**/target/surefire-reports/"

      - name: Upload coverage to Codecov
        # Only on main pushes (push from main or workflow_dispatch from main).
        # PRs and non-main branches do not upload.
        if: github.ref == 'refs/heads/main'
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./target/site/jacoco-aggregate/jacoco.xml
          fail_ci_if_error: false

  static-analysis:
    name: Static Analysis (Checkstyle + SpotBugs)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run quality profile
        run: ./mvnw $MAVEN_CLI_OPTS -Pquality -DskipTests verify

      - name: Upload SpotBugs reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: spotbugs-reports
          path: "**/target/spotbugs*.xml"

  release-build-check:
    # Validates that the codebase can produce release artifacts:
    # source JAR, Javadoc JAR, and the central-publishing bundle.
    # GPG signing and the actual deploy are excluded (those belong to
    # release.yml). Catches code-level issues like Javadoc generation
    # failures before they surface at release time.
    name: Release Build Check (sources + javadoc)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build release artifacts (no signing, no deploy)
        run: ./mvnw $MAVEN_CLI_OPTS -Prelease -DskipTests -Dgpg.skip=true verify

  docs-lint:
    name: Docs Lint (markdownlint + Spectral + cspell)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Run markdownlint
        run: npx --yes markdownlint-cli2 "**/*.md" "#node_modules" "#**/target/**"

      - name: Run cspell
        run: |
          npx --yes cspell --no-progress --no-summary \
            "**/*.{md,java,xml,yml,yaml,json,properties}" \
            --exclude "node_modules/**" --exclude "**/target/**"

      - name: Lint OpenAPI spec
        run: |
          if [ -f examples/api/openapi.yaml ]; then
            npx --yes @stoplight/spectral-cli lint \
              examples/api/openapi.yaml \
              --ruleset .spectral.yaml
          else
            echo "OpenAPI spec not present; skipping Spectral."
          fi

